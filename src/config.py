# import random
# VERSION = str(random.randint(0, 99999))

VERSION = "0.1.0"
OVERRIDE_VOCAB_SIGNIFICANCE = True

# Lightweight localization for generated Anki cards.
# Supported values: "cs" (Czech, default), "en" (English).
# Used by src/anki_generator.py to translate on-card UI strings.
LANGUAGE = "cs"
